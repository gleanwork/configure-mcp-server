export function isTokenSuccess(json) {
    return (json !== undefined &&
        typeof json === 'object' &&
        (json === null || json === void 0 ? void 0 : json.token_type) == 'Bearer' &&
        'access_token' in json);
}
export function isAuthResponse(json) {
    return hasCommonAuthResponseFields(json) && 'verification_uri' in json;
}
export function isAuthResponseWithURL(json) {
    return hasCommonAuthResponseFields(json) && 'verification_url' in json;
}
function hasCommonAuthResponseFields(json) {
    return (json !== undefined &&
        typeof json === 'object' &&
        'device_code' in json &&
        'user_code' in json &&
        'expires_in' in json &&
        'interval' in json);
}
//# sourceMappingURL=types.js.map